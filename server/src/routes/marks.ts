import { Router } from 'express';

const router = Router();

// TODO: Implement marks routes
// POST /api/marks/entry - Enter student marks
// GET /api/marks/:studentId - Get student marks
// PUT /api/marks/:id - Update marks
// DELETE /api/marks/:id - Delete marks entry
// GET /api/marks/sheet/:studentId - Get marks sheet
// POST /api/marks/qr-verify - Verify marks via QR code

export default router;
